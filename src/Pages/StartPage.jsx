import { Link } from "react-router-dom";

const StartPage = () => {
  return (
    <div>
        <p>StartPage</p>
        <Link to='/artwork'>Artwork page</Link>
    </div>
  );
}

export default StartPage;
